<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.spring_redis.mapper.LotteryUserMapper">

    <insert id="insertOne">
        insert into lottery_user(user_id, lottery_id, lottery_content, create_time, alter_time) values (#{userId}, #{lotteryId}, #{lotteryContent}, #{createTime}, #{alterTime})
    </insert>

</mapper>